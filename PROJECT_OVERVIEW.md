# PitchIQ — Project Overview

> A detailed snapshot of the PitchIQ codebase, architecture, and current state.
> Generated from the working tree on `main` (June 28, 2026).

---

## 1. What it is

A football intelligence web app that turns two team names into a structured pre-match tactical report. Built for **Hack Days CUET 2026** as a 24-hour solo MVP. The report is generated by Google Gemini (`gemini-2.5-flash`), grounded against the bundled FIFA World Cup 2026 dataset, and rendered into a React dashboard.

**Tagline:** *Football Intelligence, Reimagined.*

**Flagship demo:** four pre-defined matchups — Argentina vs France, Brazil vs Germany, England vs Portugal, Japan vs Morocco.

---

## 2. Status snapshot

| Area | State |
| --- | --- |
| Frontend application | ✅ Complete |
| Backend API | ✅ Complete |
| Gemini integration | ✅ Live (`gemini-2.5-flash`) |
| Tactical match analysis endpoint | ✅ Live |
| Docker, Cloud Run, Firebase Hosting | ✅ Done |
| Tests | ✅ `pytest` green (count varies; see `pytest --collect-only -q`) |
| In progress | User Interface Evolution, production optimization (caching, telemetry, observability) |

---

## 3. Architecture at a glance

```text
React (Firebase Hosting)  ──HTTPS JSON──►  FastAPI (Cloud Run)
       │                                         │
       │ build-time                              │ runtime
       │ VITE_API_BASE_URL                       │ GEMINI_API_KEY (Secret Mgr)
       │                                         │ CORS_ORIGINS, APP_ENV, model, timeout
       │                                         ▼
       │                          AnalysisService → GeminiService → google-genai
       │                                         │
       │                                         ▼
       │                          KnowledgeService (cached JSON)
       │                                         │
       │                                         ├─ worldcup.teams.json
       │                                         ├─ worldcup.groups.json
       │                                         ├─ worldcup.squads.json
       │                                         ├─ worldcup.stadiums.json
       │                                         ├─ worldcup.json (fixtures)
       │                                         └─ worldcup.quali_playoffs.json
```

### Request flow for `POST /api/analyze`

1. FastAPI route (thin controller) validates `AnalysisRequest` with Pydantic (`homeTeam`, `awayTeam`, distinct, names 2–80 chars).
2. `AnalysisService.analyze()`:
   - Loads `tactical_analysis_system.md` prompt.
   - Calls `FootballContextBuilder.build(home, away)` to assemble a grounded knowledge block from `KnowledgeService` (only the two teams' data — no leak).
   - Renders `tactical_analysis_user.md` with `$home_team`, `$away_team`, `$football_context`.
   - Calls `GeminiService.generate_json(...)` with `response_json_schema` enforced by Gemini.
   - Validates the response against `AnalysisResponse` (Pydantic) **plus** a `_validate_match_identity` invariant: `homeTeam.name` and `awayTeam.name` must match the request, `predictedWinner ∈ {home, away, "Draw"}`.
3. On validation failure → re-issues `tactical_analysis_repair.md` with `$validation_error` and `$invalid_response` injected. Up to `GEMINI_MAX_VALIDATION_ATTEMPTS` (default 2). If all attempts fail → `502 invalid_analysis_response`.
4. Wraps in `AnalysisApiResponse { data, model }` and returns JSON.

---

## 4. Repository layout

```text
tactiq-ai/                      ← repository folder (PitchIQ)
├── README.md, AGENTS.md, DEMO_SCRIPT.md, PROJECT_OVERVIEW.md
├── docker-compose.yml          ← local dev (frontend waits for backend healthcheck)
├── firebase.json               ← SPA rewrite only (no cache headers)
├── backend/
│   ├── Dockerfile (Python 3.12-slim, non-root, PORT env)
│   ├── pyproject.toml, requirements.txt, requirements-dev.txt
│   ├── app/
│   │   ├── main.py             ← FastAPI app, CORS allowlist, exception handlers, startup log
│   │   ├── api/
│   │   │   ├── router.py       ← /api mount (health at root)
│   │   │   └── routes/         ← analysis.py, health.py
│   │   ├── config/settings.py  ← pydantic-settings, env-driven
│   │   ├── core/
│   │   │   ├── errors.py       ← AppError + Gemini/Analysis error subclasses
│   │   │   └── prompt_loader.py ← string.Template over .md files
│   │   ├── prompts/            ← versioned Markdown prompt templates
│   │   ├── schemas/            ← analysis.py, common.py
│   │   ├── services/
│   │   │   ├── analysis_service.py
│   │   │   ├── context_builder.py
│   │   │   ├── gemini_service.py
│   │   │   └── knowledge_service.py
│   │   └── knowledge/          ← bundled World Cup 2026 JSON (mounted into image)
│   ├── tests/                  ← conftest, test_api, test_analysis_service,
│   │                             test_context_builder, test_knowledge_service
│   └── scripts/smoke_matchups.py ← manual smoke runner (not part of pytest)
└── frontend/
    ├── Dockerfile (multi-stage: development / build / nginx production)
    ├── nginx.conf (SPA fallback + /healthz + immutable asset caching for /assets/)
    ├── package.json (React 19.1, Vite 6.3, TS 5.8 strict, TanStack Query 5.81,
    │                React Router 7, Tailwind v4)
    ├── vite.config.ts, tsconfig.*
    └── src/
        ├── main.tsx, app/ (App.tsx, router.tsx, providers/AppProviders.tsx)
        ├── layouts/AppLayout.tsx
        ├── pages/ (HomePage, NotFoundPage)
        ├── features/match-analysis/ (page + 12 components + useMatchAnalysis hook)
        ├── services/analysisService.ts
        ├── types/analysis.ts
        └── styles/index.css
```

---

## 5. Tech stack

- **Frontend:** React 19.1, Vite 6.3, TypeScript 5.8 strict, TanStack Query 5.81, React Router 7, Tailwind CSS v4.
- **Backend:** Python 3.12, FastAPI 0.115, Pydantic 2, pydantic-settings 2.10, Uvicorn 0.34.
- **AI:** Google Gemini (`gemini-2.5-flash`) via `google-genai` 2.10, structured JSON output via `response_json_schema`.
- **Knowledge:** Bundled FIFA World Cup 2026 JSON parsed once into frozen dataclasses (`TeamRecord`, `GroupRecord`, `StadiumRecord`, `PlayerRecord`, `SquadRecord`, `FixtureRecord`), cached via `@lru_cache`.
- **Infra:** Docker, Docker Compose, Firebase Hosting, Google Cloud Run, Google Secret Manager.

---

## 6. Knowledge layer

`KnowledgeService` loads six JSON files once at startup (`backend/knowledge/`), normalises them into typed records, and exposes:

- `resolve_team(name)` — case-insensitive, supports an alias dict (USA, South Korea, Czechia, Türkiye, IR Iran, DR Congo, Côte d'Ivoire, Cabo Verde) and a substring fuzzy fallback.
- `get_team`, `search_team`, `get_group` (accepts "A" or "Group A"), `get_squad`, `get_fixture(home, away)` (ordered, paired on either side), `get_stadium` (with city-token + substring fuzzy fallback), `all_fixtures`.
- Alias dict + canonical resolution order is carefully documented so that downstream squad lookups keep working when canonical name differs from the dataset raw label.
- Public counters: `team_count`, `group_count`, `stadium_count`, `squad_count`, `fixture_count` are logged at startup.

`FootballContextBuilder` queries **only** the two requested teams' slice — squads (top 3 per position GK/DF/MF/FW), group opponents, scheduled fixtures between them, and the venue — and emits a deterministic Markdown knowledge block injected into the prompt.

---

## 7. Prompt engineering

Three versioned Markdown templates under `backend/app/prompts/`:

- **`tactical_analysis_system.md`** — Casts Gemini as a UEFA Pro Licensed Tactical Analyst, defines the JSON-only output contract, forbids inventing injuries/lineups/transfers/live facts, mandates exact team names and 0–100 scores.
- **`tactical_analysis_user.md`** — Interpolates `$home_team`, `$away_team`, `$football_context`. Asks for possession / out-of-possession / transitions / game-state analysis.
- **`tactical_analysis_repair.md`** — Retry prompt interpolating `$validation_error` and `$invalid_response` so the model can self-correct.

`PromptLoader` uses Python `string.Template` for safe variable substitution.

---

## 8. Response schema (Pydantic, `app/schemas/analysis.py`)

Top-level `AnalysisResponse` (camelCase via `to_camel` alias generator, `extra="forbid"`, `populate_by_name=True`):

| Field | Type | Constraint |
| --- | --- | --- |
| `matchOverview` | string | 12–700 chars |
| `homeTeam` / `awayTeam` | `TeamProfile` | name + formation (2–160) + playingStyle (12–700) + tacticalIdentity (12–700) |
| `predictedWinner` | string | exact home name, exact away name, or `"Draw"` |
| `confidence` | int | 0–100 |
| `formations` | `FormationMatchup` | home + away + matchup narrative |
| `strengths` / `weaknesses` | `TeamFactors` | 2–4 entries per side, 12–700 chars each |
| `keyBattles` | array | 3–5 entries, each with `zone` + `homePlayerOrUnit` + `awayPlayerOrUnit` + `edge ∈ {home, away, even}` + `analysis` |
| `tacticalInsights` | array | 3–6 entries, `importance ∈ {critical, high, medium}` |
| `expectedGameFlow` | `GameFlow` | opening / middle / closing phases |
| `coachRecommendation` | object | home + away + `decisiveAdjustment` |
| `pressingScore`, `possessionScore`, `attackingThreat`, `defensiveStability` | `TeamScore` | home + away, each 0–100 |

Post-validation invariants (enforced in `AnalysisService._validate_match_identity`): `homeTeam.name` and `awayTeam.name` must match the request (case-insensitive); `predictedWinner` must be one of the two team names or `"Draw"`. Mismatches trigger the repair path.

---

## 9. API surface

Routes are mounted in `backend/app/api/router.py`:

```text
api_router
├── /health          ← GET (root)
└── /api/analyze     ← POST
```

> The router (`backend/app/api/router.py`) mounts the analysis endpoints at the `/api` prefix and the health endpoint at the root. The frontend nginx container additionally serves a static `/healthz` for its own platform probe — that route is owned by the static site, not the API.

| Method | Path | Status |
| --- | --- | --- |
| `GET` | `/health` | Live |
| `POST` | `/api/analyze` | Live, returns `AnalysisApiResponse` |

### Error contract

All errors return `{ "error": { "code": "...", "message": "..." } }`:

| Status | Code | Trigger |
| --- | --- | --- |
| 422 | `invalid_request` | Pydantic body validation failed (identical team names, missing fields) |
| 502 | `invalid_analysis_response` | Model failed validation on every attempt |
| 502 | `gemini_unavailable` | Provider returned error or empty body |
| 503 | `gemini_not_configured` | `GEMINI_API_KEY` is empty |

`core/errors.py` defines `AppError` plus `GeminiConfigurationError`, `GeminiProviderError`, `AnalysisGenerationError`. `main.py` registers exception handlers that map them to the correct HTTP status.

---

## 10. Frontend surface

- **Routes:** `/` (Home), `/analysis` (Match Analysis — flagship), `*` (404).
- **State:** TanStack Query `useMutation` per request, with an `AbortController` ref for cancellation on resubmit and on unmount.
- **Networking:** `requestMatchAnalysis` in `services/analysisService.ts` resolves `VITE_API_BASE_URL + /api/analyze`, has its own 30 s timeout and merges an external `AbortSignal`, throws a typed `AnalysisApiError` carrying `status`/`code`/`message`/`details`.
- **Match analysis page:** form → loading state (with team names echoed) → error state with retry → results dashboard. Results are broken into components: `MatchHeader`, `ScoreBar`, `FormationsSection`, `FactorsList`, `KeyBattles`, `GameFlow`, `CoachRecommendation`, `TeamPanel`, plus loading/error states.
- **Types:** `frontend/src/types/analysis.ts` mirrors the Pydantic schema 1:1 (camelCase), with `Importance`, `Edge`, `Score` literal unions.
- **Accessibility:** semantic HTML, visible focus, sufficient contrast, accessible loading/error/empty states per AGENTS.md.

---

## 11. Configuration

**Frontend** (`frontend/.env`):

| Variable | Purpose |
| --- | --- |
| `VITE_API_BASE_URL` | Backend origin embedded in the browser bundle at build time |

**Backend** (`backend/.env`):

| Variable | Default | Purpose |
| --- | --- | --- |
| `APP_ENV` | `development` | Hides `/docs` and `/redoc` when `production` |
| `CORS_ORIGINS` | `http://localhost:5173` | Comma-separated allowlist |
| `PORT` | `8080` | Container port (Cloud Run injects this) |
| `GEMINI_API_KEY` | empty | Empty → 503 `gemini_not_configured` |
| `GEMINI_MODEL` | `gemini-2.5-flash` | |
| `GEMINI_TEMPERATURE` | `0.25` | Validated 0–1 |
| `GEMINI_MAX_VALIDATION_ATTEMPTS` | `2` | Validated 1–3 |
| `GEMINI_TIMEOUT_SECONDS` | `45` | Validated 5–300 |

Secrets are never committed; production `GEMINI_API_KEY` lives in Google Secret Manager and is mounted via `--set-secrets`.

---

## 12. Testing & verification

- **Backend:** `pytest` — full suite green. Coverage: API routes, analysis service retry/repair logic, context builder formatting, knowledge service resolution/aliases. No network dependency. Test count grows as the suite grows; the single source of truth is `pytest --collect-only -q`.
- **Smoke:** `python scripts/smoke_matchups.py` — deterministic stub generator exercises the four flagship matchups (team-name round-trip, predicted-winner set, scores in range, array length bounds, malformed-payload rejection). Not a pytest module; treat as a manual runner.
- **Frontend:** `npm run typecheck` (strict TS), `npm run build` (Vite production build).
- **End-to-end readiness checklist:** all items checked in README — pytest green, typecheck clean, build clean, smoke PASS, `firebase.json` SPA rewrite verified (no cache headers; immutable caching lives in `frontend/nginx.conf` for the Docker image), backend Dockerfile honours `PORT` and runs non-root, multi-stage frontend Dockerfile injects `VITE_API_BASE_URL`, nginx SPA fallback + `/healthz`, CORS restricted to `GET`/`POST`/`OPTIONS`, secrets kept out of repo.

---

## 13. Deployment

**Backend — Cloud Run:**

```bash
gcloud run deploy tactiq-api \
  --source backend \
  --region YOUR_REGION \
  --allow-unauthenticated \
  --set-env-vars APP_ENV=production,CORS_ORIGINS=https://YOUR_PROJECT.web.app \
  --set-secrets GEMINI_API_KEY=GEMINI_API_KEY:latest
```

**Frontend — Firebase Hosting:**

```bash
npm --prefix frontend run build
firebase use --add
firebase deploy --only hosting
```

---

## 14. Engineering guardrails (`AGENTS.md`)

- `frontend/` and `backend/` deploy independently.
- FastAPI never serves frontend assets.
- API origins and secrets in env vars only; `.env` is gitignored.
- No auth, databases, queues, or new endpoints without explicit approval.
- Routes are thin HTTP controllers; services own use-case orchestration; the Gemini client owns provider calls; prompt modules own prompt text; Pydantic schemas validate every model response.
- Strict TypeScript; no `any` to bypass design. Type all Python function signatures and module boundaries.
- Components must include loading, empty, and error states.
- Conventional Commits; focused commits only.
- Definition of done: contracts typed, errors handled, tests pass, docs/env examples current, secrets external, deployment assumptions still hold.

---

## 15. What's intentionally left for later

- **User Interface Evolution:** The current UI is functional, clean, and suitable for demonstrating the product. During the hackathon, development time was intentionally prioritized toward backend architecture, AI integration, structured response validation, reliability, and Google Cloud deployment. As a result, visual polish, animations, advanced UX refinements, and overall interface design were intentionally deferred. Future development will focus on creating a more refined, modern, and polished user experience while preserving the existing functionality.
- **Production optimisation:** response caching, telemetry, observability.
- The README earlier mentioned a future `worldcup.fixtures.json` placeholder; fixtures are now bundled in `worldcup.json` alongside the group table. Future enrichment of `worldcup.quali_playoffs.json` with additional slots is scoped for post-hackathon work.

---

## 16. Key files quick reference

| File | Why it matters |
| --- | --- |
| `backend/app/main.py` | FastAPI app, CORS, exception handlers, startup log |
| `backend/app/api/router.py` | Route composition |
| `backend/app/api/routes/analysis.py` | Thin HTTP controller; DI wiring of `AnalysisService` |
| `backend/app/services/analysis_service.py` | Retry / repair orchestration + identity invariants |
| `backend/app/services/gemini_service.py` | `google-genai` client, JSON schema, error mapping |
| `backend/app/services/context_builder.py` | Grounded knowledge block for the prompt |
| `backend/app/services/knowledge_service.py` | Cached JSON loader + typed lookups + aliases |
| `backend/app/schemas/analysis.py` | Request / response Pydantic models |
| `backend/app/prompts/tactical_analysis_*.md` | Versioned prompt templates |
| `backend/app/core/errors.py` | `AppError` hierarchy mapped to HTTP |
| `frontend/src/app/router.tsx` | Client-side routes |
| `frontend/src/features/match-analysis/MatchAnalysisPage.tsx` | Flagship feature shell |
| `frontend/src/features/match-analysis/hooks/useMatchAnalysis.ts` | TanStack Query mutation + abort control |
| `frontend/src/services/analysisService.ts` | Typed fetch + timeout + typed `AnalysisApiError` |
| `frontend/src/types/analysis.ts` | Mirror of the Pydantic schema |
| `docker-compose.yml` | Local dev with healthcheck-gated startup |
| `firebase.json` | SPA rewrite only; immutable asset caching lives in `frontend/nginx.conf` (the Docker image), not in `firebase.json` |
